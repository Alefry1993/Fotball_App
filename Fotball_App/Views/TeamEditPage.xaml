<Page
    x:Class="Fotball_App.Views.TeamEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fotball_App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBox VerticalAlignment="Center" PlaceholderText="Enter Teams Name" Text="{x:Bind TeamEditViewModel.TeamName, Mode=TwoWay}" Style="{ThemeResource TitleTextBlockStyle}" />
        <StackPanel Orientation="Horizontal" Padding="0,15,0,0">
            <Image Source="{x:Bind TeamEditViewModel.ProfileImageFullPath, Mode=OneWay}" />
            <StackPanel Padding="15,0,0,0">
                <TextBlock Text="Description" Style="{StaticResource DetailSubTitleStyle}" />
                <TextBox Width="300" PlaceholderText="Enter team description" Text="{x:Bind TeamEditViewModel.Description, Mode=TwoWay}" Style="{StaticResource DetailBodyBaseMediumStyle}" />


            </StackPanel>
        </StackPanel>
        <TextBlock Margin="{StaticResource MediumTopMargin}" Foreground="Red" VerticalAlignment="Center" Text="{x:Bind TeamEditViewModel.Errors, Mode=OneWay}" Style="{ThemeResource DetailBodyStyle}" />
    </StackPanel>
</Page>
